# DeepSeek Harness 桌面版开发目标

## 最终目标

交付一个可独立安装、可日常使用的 macOS 原生桌面软件：它在本机运行真实的 DeepSeek Harness 服务，并直接呈现与当前官方 Harness Web 同源的完整 GUI。用户无需安装 Node、pnpm、Git 或 Harness，即可完成对话、会话管理、模型与权限设置、Agent 预设和插件管理。

该产品不是远程网页壳、静态页面复刻，也不是在官方 GUI 之外另做一个控制中心。Electron 原生层只负责应用生命周期、服务托管、窗口、菜单、托盘、更新与安全恢复；产品功能和交互统一在 Harness 官方 GUI 内实现。

## 目标用户与使用结果

目标用户是需要在 macOS 本机稳定使用 DeepSeek Harness 的开发者与高级 Agent 使用者。安装 DMG 并配置 API Key 后，用户应能在一个简体中文的原生应用中使用 Harness 的全部官方 Web 能力，并在设置页完成插件的发现、安装、更新、卸载、启用、停用与配置，而不需要打开终端。

## 产品要求

| 范畴 | 达成标准 |
| --- | --- |
| 官方 GUI 同源 | 主窗口加载本机真实 `dsh web`，不维护重复的聊天、设置或 Agent 业务界面；随 Harness Web 更新同步其可用功能。 |
| 中文与桌面体验 | 新安装默认简体中文；菜单、原生提示和错误信息均使用简体中文；使用黑色 DeepSeek 图标；不显示无实际用途的内测声明。 |
| 自包含运行时 | 安装包内提供匹配版本的 Node、已构建 Harness 及插件生命周期所需的 pnpm；干净 macOS 环境不依赖系统 Node、pnpm、Git 或全局 Harness。 |
| 服务可靠性 | 应用负责启动、健康检查、停止和异常恢复本地 Harness；升级使用暂存校验、原子替换和失败回退，不破坏可运行的旧版本与用户数据。 |
| 一等插件中心 | 在官方 GUI 的设置页中展示插件来源、版本、状态与可用操作；支持安装、更新、卸载、启用、停用和配置 Schema 表单，并在需要时引导重载受影响的 Profile。 |
| 安全与数据 | 凭据、会话、模型与用户设置沿用 Harness 本地数据模型；原生层不重复保存 API Key；插件操作遵循 Harness 的 Profile、权限与兼容性校验。 |

## 可插拔能力的定义

“全部可插拔”指 Harness 的能力以插件和 Profile 组合运行，用户可通过 GUI 管理与其当前 Profile 兼容的插件生命周期，而不是把所有任意 npm 包无条件装入运行环境。GUI 必须对不兼容、缺少权限、操作失败、重启后生效和预设内置挂载等状态给出明确反馈；插件实际安装结果以 Harness 后端返回为准。

Agent 预设挂载的插件、用户直接修改的配置层和 GUI 管理的插件必须共用同一份 Harness 配置来源，避免 GUI 生成平行配置或覆盖用户已有挂载。

## 开发能力边界

桌面 GUI 的职责是完整承载 Harness 的真实开发工作流：用户可在同源 GUI 中创建任务、配置模型与权限、调用 Agent、管理会话、使用工具并通过插件扩展能力。它不是一个会自行改写桌面应用源码的自治开发器；桌面端自身的版本迭代仍由受控候选构建、预览验证、推广前的候选进程与 Harness 服务复验，以及用户确认发布。这样既保留 Harness 面向开发任务的能力，也避免 Agent 在无确认条件下篡改已安装应用。

## 当前基线与近期开发目标

当前桌面端已能在隔离环境启动真实 Harness 并显示中文官方 GUI。后续第一优先级是闭合干净安装环境中的插件生命周期：

1. 在打包与发布流程内置固定版本 pnpm，并在启动 Harness 时通过 `DSH_PNPM_BIN` 传入其绝对路径。
2. 确保安装、更新、卸载、启用、停用和配置均通过 Harness 的插件管理服务执行，界面状态与真实 Profile 保持一致。
3. 在没有系统 pnpm 的隔离环境中，分别验证上述六类插件操作及失败反馈。
4. 补齐插件中心对 Web Profile、Agent 预设挂载和用户已有配置层的可见性与不覆盖策略。

## 不在本目标内

- 不将远程网站嵌入 Electron 作为主功能。
- 不复制或重写官方 Harness 的聊天、设置、模型、插件和 Agent 业务界面。
- 不静默替换用户已安装的应用；候选构建、预览、健康检查和用户确认均为升级前置条件。
- 不绕过 Harness 的权限、Profile、插件兼容性或凭据保护机制。

## 验收标准

完成时必须同时满足以下条件：

1. 新机器仅安装 DMG 后即可启动官方 Harness GUI，并完成 API Key 配置和一次真实会话。
2. 首屏、设置页、模型页、Agent 预设页和插件中心均为简体中文可操作界面；不存在内测声明。
3. 在无系统 pnpm、Node、Git 和 Harness 的隔离环境中，GUI 插件安装、更新、卸载、启用、停用与配置均有成功、失败和重试验证证据。
4. 应用重启、Harness 异常退出、升级失败和候选版本启动失败时，服务与用户数据可恢复，旧版应用仍可用。
5. 桌面自动化、构建校验、打包态启动和 DMG 完整性校验全部通过；发布前提供命令输出、截图或日志等可复核证据。
